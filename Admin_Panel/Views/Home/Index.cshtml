@{    ViewBag.Title = "Index";    Layout = "~/Views/Shared/_Layout.cshtml";}<!-- Main content --><!-- Your HTML content --><section class="content">    <div class="container-fluid">        <div class="row">            <div class="col-md-12">                <div class="card card-primary">                    <div class="card-header">                        <h3 class="card-title">Add Records</h3>                    </div>                    <form onsubmit="submitForm(); return false;">                        <div class="card-body">                            <div class="row">                                <div class="col-md-4">                                    <div class="form-group">                                        <label for="ID">Workstation ID <span style="color:red;">*</span></label>                                        <div class="input-group">                                            <input type="text" id="ID" placeholder="Enter Workstation ID" class="form-control Field">                                            <div class="input-group-append">                                                <button class="btn btn-primary" type="button" data-toggle="modal" data-target="#VendorSearchModal">                                                    <i class="fas fa-search"></i>                                                </button>                                            </div>                                            <div class="input-group-append">                                                <button type="button" class="btn btn-success" onclick="checkValue()">                                                    <i class="fas fa-plus"></i>                                                </button>                                            </div>                                        </div>                                    </div>                                </div>                                <div class="col-md-8">                                    <div class="form-group">                                        <label for="name">Workstation Name <span style="color:red;">*</span></label>                                        <input id="name" type="text" class="form-control Field" placeholder="Enter Workstation Name">                                    </div>                                </div>                            </div>                            <div class="row">                                <div class="col-md-6">                                    <div class="form-group">                                        <label>Type of Work <span style="color:red;">*</span></label>                                        <select class="form-control select2" style="width: 100%;" id="worktype">                                            <option selected="selected">Type 1</option>                                            <option>Type 1</option>                                            <option>Type 2</option>                                            <option>Type 3</option>                                            <option>Type 4</option>                                            <option>Type 5</option>                                            <option>Type 6</option>                                        </select>                                    </div>                                </div>                                <div class="col-md-6">                                    <div class="form-group">                                        <label>Department <span style="color:red;">*</span></label>                                        <select class="form-control select2" style="width: 100%;" id="department">                                            <option selected="selected">Department 1</option>                                            <option>Department 1</option>                                            <option>Department 2</option>                                            <option>Department 3</option>                                            <option>Department 4</option>                                            <option>Department 5</option>                                            <option>Department 6</option>                                        </select>                                    </div>                                </div>                            </div>                            <div class="row">                                <div class="col-md-3">                                    <div class="form-group">                                        <label>Clearing Time <span style="color:red;">*</span></label>                                        <input type="time" class="form-control" id="appt" name="appt">                                    </div>                                </div>                            </div>                            <div class="row">                                <div class="col-md-1">                                    <div class="form-group">                                        <label for="name">Labour:</label>                                    </div>                                </div>                                <div class="col-md-2">                                    <div class="custom-control custom-checkbox">                                        <input class="custom-control-input" type="checkbox" id="customCheckbox2" value="option2 " onchange="toggleSkillFields()">                                        <label for="customCheckbox2" class="custom-control-label">Skilled<span style="color:red;">*</span></label>                                    </div>                                </div>                                <div class="col-md-2">                                    <div class="custom-control custom-checkbox">                                        <input class="custom-control-input" type="checkbox" id="customCheckbox3" value="option3" onchange="toggleSkillFields()">                                        <label for="customCheckbox3" class="custom-control-label">Unskilled<span style="color:red;">*</span></label>                                    </div>                                </div>                                                                                            </div>                            <div class="row">                                <div class="col-md-1">                                    <div class="form-group">                                        <label for="name">Status:</label>                                    </div>                                </div>                                <div class="col-md-2">                                    <div class="custom-control custom-checkbox">                                        <input class="custom-control-input" type="checkbox" id="customCheckbox4" value="option4">                                        <label for="customCheckbox4" class="custom-control-label">Available<span style="color:red;">*</span></label>                                    </div>                                </div>                                <div class="col-md-2">                                    <div class="custom-control custom-checkbox">                                        <input class="custom-control-input" type="checkbox" id="customCheckbox5" value="option5">                                        <label for="customCheckbox5" class="custom-control-label">Unavailable<span style="color:red;">*</span></label>                                    </div>                                </div>                            </div>                            <div class="row">                                <div class="col-md-2">                                    <div class="form-group">                                        <label id="fromLabel" style="display: none;"> From <span style="color:red;">*</span></label>                                        <input type="date" class="form-control" id="startdate" name="startdate">                                    </div>                                </div>                                <div class="col-md-2">                                    <div class="form-group">                                        <label id="toLabel" style="display: none;">To <span style="color:red;">*</span></label>                                        <input type="date" class="form-control" id="enddate" name="enddate">                                    </div>                                </div>                            </div>                        </div>                        <button id="submit" type="button" class="btn btn-primary" onclick="submitForm()" style="margin-left:10px;">Save</button>                        <button type="button" id="btnclose" class="btn btn-secondary" cf_name="cancel" value="Cancel" onClick="closebox()" style="margin-left:10px">Close</button>                </div>                <!-- /.card-body -->                </form>            </div>        </div>    </div>    </div></section><div class="card">    <div class="card-header">        <h3 class="card-title">DataTable </h3>    </div>    <!-- /.card-header -->    <div class="card-body">        <table id="dataTable" class="table table-bordered table-striped">            <thead>                <tr>                    <th>ID</th>                    <th>Name</th>                    <th>Status</th>                    <th>From Date</th>                    <th>To Date</th>                    <th>Functions</th>                </tr>            </thead>        </table>    </div>    <!-- /.card-body --></div><script>    function submitForm() {
    // Get values from form fields
    var id = document.getElementById("ID").value;
    var name = document.getElementById("name").value;
    var available = document.getElementById("customCheckbox4").checked ? "Available" : "Unavailable";
    var startDate = document.getElementById("startdate").value;
    var endDate = document.getElementById("enddate").value;

    // Create a new row in the table and append data
    var table = document.getElementById("dataTable");
    var newRow = table.insertRow(table.rows.length);
    var cells = newRow.insertCell(0);
    cells.innerHTML = id;
    cells = newRow.insertCell(1);
    cells.innerHTML = name;
    cells = newRow.insertCell(2);
    cells.innerHTML = available;

    // Add "From" and "To" dates to the table if "Unavailable" is checked
    if (available === "Unavailable") {
        cells = newRow.insertCell(3); //  index 3 for "From" date
        cells.innerHTML = startDate;
        cells = newRow.insertCell(4); //  index 4 for "To" date
        cells.innerHTML = endDate;
    } else {
        // empty cells for "From" and "To" dates if "Available" is checked
        cells = newRow.insertCell(3);
        cells = newRow.insertCell(4);
    }

    cells = newRow.insertCell(5); 

    //  icons for Delete and Add functions
        cells.innerHTML = `<i class="fas fa-trash-alt" onclick="deleteRow(this)" style="color: red; font-size: 20px;"></i>
                            <i class="fas fa-plus" onclick="addFunction(this)" style="color: white; background-color: green; border-radius: 50%; display: inline-block; padding: 8px;"></i>`;

    // Clear form fields
    document.getElementById("ID").value = "";
    document.getElementById("name").value = "";
    document.getElementById("customCheckbox4").checked = false;
    document.getElementById("customCheckbox5").checked = false;
    document.getElementById("startdate").value = "";
    document.getElementById("enddate").value = "";
    // Hide the "From" and "To" fields if "Available" is checked
    toggleDateFields();
}
    function closebox() {        location.reload();    }    function deleteRow(icon) {        var row = icon.parentNode.parentNode;        row.parentNode.removeChild(row);    }    function addFunction(icon) {        // Your logic for the "Add" function here        alert("Add function clicked");    }    // Function to toggle visibility of "From" and "To" fields based on the "Unavailable" checkbox    function toggleDateFields() {        var startDateField = document.getElementById("startdate");        var endDateField = document.getElementById("enddate");        var fromLabel = document.getElementById("fromLabel");        var toLabel = document.getElementById("toLabel");        var unavailableCheckbox = document.getElementById("customCheckbox5"); // Change to "customCheckbox5"        if (unavailableCheckbox.checked) {            fromLabel.style.display = "block";            toLabel.style.display = "block";            startDateField.style.display = "block";            endDateField.style.display = "block";        } else {            fromLabel.style.display = "none";            toLabel.style.display = "none";            startDateField.style.display = "none";            endDateField.style.display = "none";        }    }    function toggleSkillFields() {        var skilledCheckbox = document.getElementById("customCheckbox5");        var unskilledCheckbox = document.getElementById("customCheckbox6");        // Ensure only one checkbox is checked at a time        skilledCheckbox.checked = unskilledCheckbox.checked ? false : skilledCheckbox.checked;        unskilledCheckbox.checked = skilledCheckbox.checked ? false : unskilledCheckbox.checked;    }    // Call the function initially to hide the "From" and "To" fields    toggleDateFields();    // Attach the function to the change event of the "Unavailable" checkbox    document.getElementById("customCheckbox5").addEventListener("change", toggleDateFields);</script> @*//function submitForm() { //    // Get values from form fields //    var id = document.getElementById("ID").value; //    var name = document.getElementById("name").value; //    var available = document.getElementById("customCheckbox4").checked ? "Available" : "Unavailable"; //  /*  var clearingTime = document.getElementById("appt").value;*/ //    var startDate = document.getElementById("startdate").value; //    var endDate = document.getElementById("enddate").value; //    //var skilled = document.getElementById("customCheckbox2").checked ? "Yes" : "No"; // Added this line for skilled checkbox //    //var unskilled = document.getElementById("customCheckbox3").checked ? "Yes" : "No"; // Added this line for unskilled checkbox //    // Create a new row in the table and append data //    var table = document.getElementById("dataTable"); //    var newRow = table.insertRow(table.rows.length); //    var cells = newRow.insertCell(0); //    cells.innerHTML = id; //    cells = newRow.insertCell(1); //    cells.innerHTML = name; //    //cells = newRow.insertCell(2); //    //cells.innerHTML = clearingTime; //    cells = newRow.insertCell(3); //    cells.innerHTML = available; //    //cells = newRow.insertCell(4); // Index 4 for "Skill" column //    //cells.innerHTML = skilled === "Yes" ? "Yes" : (unskilled === "Yes" ? "No" : ""); // Populate "Skill" column based on checkboxes //    // Add "From" and "To" dates to the table if "Unavailable" is checked //    if (available === "Unavailable") { //        cells = newRow.insertCell(5); // Index 5 for "From" date //        cells.innerHTML = startDate; //        cells = newRow.insertCell(6); // Index 6 for "To" date //        cells.innerHTML = endDate; //    } else { //        // Add empty cells for "From" and "To" dates if "Available" is checked //        cells = newRow.insertCell(5); //        cells = newRow.insertCell(6); //    } //    cells = newRow.insertCell(7); // Adjust the index based on your table structure //    // Add icons for Delete and Add functions //    cells.innerHTML = `<i class="fas fa-trash-alt" onclick="deleteRow(this)"></i> //                   <i class="fas fa-plus" onclick="addFunction(this)"></i>`; //    // Clear form fields //    document.getElementById("ID").value = ""; //    document.getElementById("name").value = ""; //    document.getElementById("customCheckbox4").checked = false; //    document.getElementById("customCheckbox5").checked = false; //    //document.getElementById("customCheckbox2").checked = false; // Clear skilled checkbox //    //document.getElementById("customCheckbox3").checked = false; // Clear unskilled checkbox //    document.getElementById("startdate").value = ""; //    document.getElementById("enddate").value = ""; //    // Hide the "From" and "To" fields if "Available" is checked //    toggleDateFields(); //}*@@*<table style="width:100%" id="dataTable">        <tr>            <th>ID</th>            <th> Name</th>            <th>Type of Work</th>            <th>Department</th>            <th>Labour</th>            <th>Skilled</th>            <th>UnSkilled</th>            <th>Clearing Time</th>            <th>Availability</th>            <th>Unavailable From</th>        </tr>    </table>    <script>        function submitForm() {            // Get values from form fields            var id = document.getElementById("ID").value;            var name = document.getElementById("name").value;            var worktype = document.getElementById("worktype").value;            var department = document.getElementById("department").value;            var skilled = document.getElementById("customCheckbox2").checked ? "Yes" : "No";            var unskilled = document.getElementById("customCheckbox3").checked ? "Yes" : "No";            var clearingTime = document.getElementById("appt").value;            var available = document.getElementById("customCheckbox4").checked;            var startDate = document.getElementById("startdate").value;            var endDate = document.getElementById("enddate").value;            // Create a new row in the table and append data            var table = document.getElementById("dataTable");            var newRow = table.insertRow(table.rows.length);            var cells = newRow.insertCell(0);            cells.innerHTML = id;            cells = newRow.insertCell(1);            cells.innerHTML = name;            cells = newRow.insertCell(2);            cells.innerHTML = worktype;            cells = newRow.insertCell(3);            cells.innerHTML = department;            cells = newRow.insertCell(4);            cells.innerHTML = skilled;            cells = newRow.insertCell(5);            cells.innerHTML = unskilled;            cells = newRow.insertCell(6);            cells.innerHTML = clearingTime;            cells = newRow.insertCell(7);            cells.innerHTML = available ? "Available" : "Unavailable";            cells = newRow.insertCell(8);            cells.innerHTML = available ? "" : startDate + " To " + endDate;            // Clear form fields            document.getElementById("ID").value = "";            document.getElementById("name").value = "";            document.getElementById("worktype").value = "Type 1";            document.getElementById("department").value = "Department 1";            document.getElementById("customCheckbox2").checked = false;            document.getElementById("customCheckbox3").checked = false;            document.getElementById("appt").value = "";            document.getElementById("customCheckbox4").checked = false;            document.getElementById("customCheckbox5").checked = false; // Added to clear the "Unavailable" checkbox            document.getElementById("startdate").value = "";            document.getElementById("enddate").value = "";            // Hide the "From" and "To" fields if "Available" is checked            toggleDateFields();        }        function closebox() {            location.reload();        }        // Function to toggle visibility of "From" and "To" fields based on the "Unavailable" checkbox        function toggleDateFields() {            var startDateField = document.getElementById("startdate");            var endDateField = document.getElementById("enddate");            var fromLabel = document.getElementById("fromLabel");            var toLabel = document.getElementById("toLabel");            var unavailableCheckbox = document.getElementById("customCheckbox5"); // Change to "customCheckbox5"            if (unavailableCheckbox.checked) {                fromLabel.style.display = "block";                toLabel.style.display = "block";                startDateField.style.display = "block";                endDateField.style.display = "block";            } else {                fromLabel.style.display = "none";                toLabel.style.display = "none";                startDateField.style.display = "none";                endDateField.style.display = "none";            }        }        // Call the function initially to hide the "From" and "To" fields        toggleDateFields();        // Attach the function to the change event of the "Unavailable" checkbox        document.getElementById("customCheckbox5").addEventListener("change", toggleDateFields);    </script>*@